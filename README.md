# Multikube tool
```
ks - List Kubernetes clusters
ks [context] - Set current Kubernetes clustr
```

```
ka - run kubectl on multiple clusters
ka "kubectl get pods -n namespace" - Get pods from namespace from all Kubernetes clusters
```
